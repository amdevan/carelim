import { Router } from "express";

const router = Router();

// Auth routes will be migrated here from Next.js API routes
// router.post("/login", loginHandler);
// router.post("/admin-auth", adminAuthHandler);
// router.post("/doctor-auth", doctorAuthHandler);

export default router;
